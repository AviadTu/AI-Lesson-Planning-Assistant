# lesson-agent dependencies.
# Versions are intentionally unpinned; pin them (pip freeze) before release.

# ── Web framework ────────────────────────────────────────────────────
fastapi
uvicorn[standard]
pydantic-settings

# ── Conversational reasoning (real LangGraph + LangChain) ────────────
langgraph
langgraph-checkpoint-sqlite   # persistent per-session conversation state
langchain-core
langchain-ollama              # LOCAL model (classification / missing-info)

# ── Cloud generation transport ───────────────────────────────────────
httpx                         # direct Ollama Cloud API (OS trust store + Bearer)

# ── Optional observability (feature-flagged; import is lazy) ─────────
# Install only if you enable Langfuse tracing (LANGFUSE_ENABLED=true). The app
# runs without it — tracing is a strict no-op when the SDK/keys are absent.
# langfuse
