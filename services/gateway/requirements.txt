# FastAPI Gateway dependencies.
# Versions are intentionally unpinned; pin them (pip freeze) before release.
fastapi
uvicorn[standard]
httpx
python-multipart
jinja2
pydantic-settings

# Local Hebrew/RTL PDF generation (lesson-plan export).
fpdf2
uharfbuzz        # HarfBuzz text shaping — correct Hebrew bidi/RTL

# Lesson-source document extraction (/internal/extract; DOCX for n8n).
pypdf
python-docx
